// React subpath entry point: re-exports the hook so React stays out of the core.
export { useVoiceChanger } from "./useVoiceChanger";
export type {
  UseVoiceChangerOptions,
  UseVoiceChangerResult,
} from "./useVoiceChanger";
