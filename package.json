{
  "name": "browser-voip-dsp",
  "version": "0.1.0",
  "private": true,
  "description": "Browser VoIP DSP preprocessing for AI voice agents — local echo cancellation, noise suppression, VAD, DTMF and audio effects compiled to WebAssembly and run inside an AudioWorklet.",
  "type": "module",
  "scripts": {
    "build:wasm": "node scripts/build-wasm.mjs",
    "build:vc": "node voice-changer/scripts/build-wasm.mjs",
    "predev": "npm run build:wasm && npm run build:vc",
    "dev": "vite",
    "prebuild": "npm run build:wasm && npm run build:vc",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
